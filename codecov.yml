# Coverage excludes code that unit tests do not meaningfully cover:
#   - internal/mgmtapi: generated by oapi-codegen from api/openapi.yaml.
#   - cmd: the composition root; linux-only wiring exercised end to end, not
#     by unit tests.
ignore:
  - "internal/mgmtapi/**"
  - "cmd/**"
